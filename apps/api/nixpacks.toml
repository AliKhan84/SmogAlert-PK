[phases.setup]
nixPkgs = ["python312", "gcc", "zlib", "libffi", "openssl"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r requirements.txt",
  "pip install --no-cache-dir 'pyarrow>=15.0'",
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
